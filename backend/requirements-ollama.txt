# Requirements for environments that include ollama support
# WARNING: These versions conflict with the base requirements.txt
# Only use this if you specifically need ollama integration.
# Do NOT install both requirements.txt and requirements-ollama.txt in the same venv.

# Core FastAPI dependencies (ollama-compatible versions)
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic>=2.9,<3
pydantic-settings>=2.1.0
python-multipart==0.0.6

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
httpx>=0.27,<0.29

# LLM provider SDKs
openai==1.3.7
anthropic==0.7.8
google-generativeai==0.3.1
ollama>=0.1.0
